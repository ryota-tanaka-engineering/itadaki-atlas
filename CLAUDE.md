@AGENTS.md

# Itadaki Atlas

日本食を超細分類し、歴史と発祥地を日本地図にマッピングして紹介するWebサイト。主対象は海外（英語圏）ユーザー。将来的にはブランド牛・地鶏・酒・土産品まで含む「**日本の食の地理データベース**」を目指す。

## North Star

全社の「信頼関係の総量最大化」を、本プロダクトでは**3層の信頼**として具体化する。

読者（正確で深い）／事業者（中立な媒体）／送客先（薦めた先だから間違いない）——**3つ揃って初めて送客が成立する**。

- **KGI は PV ではなく「既存事業への送客数」。** PVを頂点に置くとバズ狙いへ判断が歪むため
- 判断に迷ったら **正確さ > 網羅性 > 速度**
- 詳細: `.doc/00_concept/01_north_star.md`

## 規約

- **実装規約: `ia-nextjs-standards` Skill**（コードを書く前に必ず参照）
- **ドキュメント: `ia-doc-sync` Skill**（`.doc` の差分主義・SSOT）
- マイグレーション: `supabase-migration` Skill
- **コンテンツを足す: `ia-atlas-add` Skill**（何を足すときもこの一本。構造の問い→部隊→1コマンド投入→機械検査→導線検品→本番）。**何を書くかの規約: `ia-atlas-content` Skill**（採否・文体・3種別・Tier・データ規律）
- 共通方針（インフラ・i18n・型生成・セキュリティ）は Platform `../.doc/` が正。**Product側に再掲しない**

## コマンド

| コマンド | 内容 |
| :--- | :--- |
| `npm run dev` | 開発サーバー（**ポート3300**。3000=deep-local / 3100=oyster-media / 3200=food-recommend と衝突回避） |
| `npm run lint` | ESLint |
| `npx tsc --noEmit` | 型チェック |
| `npm run test` | Vitest 単体（`src/**/*.test.ts(x)` にソース併置） |
| `npm run test:watch` | Vitest ウォッチ |
| `npm run test:e2e` | Playwright E2E（`tests/`。devサーバーは自動起動） |
| `npm run build` | 本番ビルド |
| `NEXT_OUTPUT=standalone npm run build` | ポータビリティ規約の確認用 |

## 現在の状態

**確定デザイン・確定情報構造で全ページ型実装済み・本番稼働中（workers.dev、独自ドメイン未設定）。** データ投入だけでサイトが伸びる。投入済み（2026-09-10、ローカル＝本番）: 棚28・ジャンル21（ラーメン61/焼き鳥25/寿司110/和牛43/うどん26/そば25/米の品種37/発酵調味料20/地鶏35/銘柄豚23/牛肉の部位30/豚肉の部位20/魚56/貝・甲殻類・イカタコ35/丼物28/揚げ物48/焼きそば23/漬物24/粉もの23/練り物24/寿司ネタ27）+ その他1127件 = **1867件**・47都道府県・関係1909本。うち農林水産省「うちの郷土料理」の悉皆（47都道府県・約1,180件）は **Tier1（概要・出典・発祥地・関係）** で投入済みで、本文3章は未投入（`content:lint` の W1 = 1186件。次は県ごとの代表品から厚くする）。それ以外は全件に本文3章と出典あり。行き止まりゼロ（`npm run content:lint --strict` で機械検査）。ジャンルは `genres.type` で3層（dish 料理／ingredient 食材／cut 部位・ネタ）に分かれ、トップの種類カードもこの3層で出す。

2026-09: 全国区の料理（焼き鳥・餃子・おでん・うなぎ蒲焼・すき焼き・唐揚げ・カレーライス・握り寿司・天ぷら等）は発祥地なしの受け皿アイテムとして持ち、複数の「本場」で土地に結びつける（体験原則3・5）。詳細ページには部位図（牛・豚・鶏のSVG線画、`src/features/map/CutDiagram.tsx`）と系統図（関係データから描く `LineageTree.tsx`）を本文冒頭に置く。計測は GA4（同意バナー、許可時のみ）+ Cloudflare Web Analytics（`src/components/Analytics.tsx`。IDは `.env.production.local`）。「食べに行く前に」ガイド（マナー・注文・支払い等。`guides` 4テーブル、`/guide`、詳細ページに関連ガイド）と地名の英語表記（`place_names`）は最初から多言語前提のテーブル設計。正典は `ia-atlas-content` Skill §2.4・§3、スキーマは `.doc/20_data/01_models.md` §3.6/§3.9。

| ページ型 | 状態 |
| :--- | :--- |
| トップ（明色地図 + シート: 検索入口 + 土地/種類/興味の3カード） | ✅ |
| `/[genre]`（ジャンル: 系統チップ+一覧+CTA）／同URLで棚ページ（主要ジャンル+その他図鑑） | ✅ |
| `/[genre]/[slug]`（詳細: 橙カバー・位置帯・目次+章Markdown・つながり2軸。棚slug経由でその他も到達可） | ✅ |
| `/region/[pref]`（●生まれた/■育てる/◆仕込むの3群 + 隣接県） | ✅ |
| `/tags`・`/tag/[slug]`（タグ横断） | ✅ |
| `/chain/[slug]`（チェーン独立ページ。SEO流入起点→ご当地へ橋渡し） | ✅ |
| `/guide`・`/guide/[slug]`（食べに行く前に: 注文／支払い／マナー／店の見つけ方／持ち帰り／季節） | ✅（3件。量産は次） |
| About/利用規約/プライバシー/問い合わせ（Supabase保存）・共通ヘッダー/フッター | ✅ |
| OGP共有カード・i18n（/ja /en・hreflang・sitemap） | ✅ |

| 項目 | 状態 |
| :--- | :--- |
| デプロイ先 | **Cloudflare Workers 稼働中**（2026-09 初回デプロイ。https://itadaki-atlas.itadaki-atlas.workers.dev 。デプロイは `scripts/deploy-prod.sh`（タイル退避 + 本番環境変数注入 + `npm run deploy:cf`）。計測 ID は `.env.production.local` に置く。タイルは R2 `itadaki-atlas-tiles` の r2.dev 公開URL） |
| Supabase 本番 | **稼働中**（ref: xzkvvdldovgbuutttmzj・東京・Free。ローカルと同一データを投入済み） |
| ドメイン `itadakiatlas.com` / Sentry / GitHub自動デプロイ（CF_DEPLOY_ENABLED） | **未設定**（ドメイン取得後に SITE_URL 設定・R2カスタムドメイン化・workers.dev 無効化をまとめて行う） |
| マーク（ロゴ） | **未決**（暫定=三つ紋。ブランドページの3案から選定待ち） |

次の作業候補: /security-review（本格公開前に必須）、独自ドメイン化一式（itadakiatlas.com 取得待ち）、計測IDの設定、`ia-atlas-deploy` Skill 作成、ラーメン協会一覧との差分109件（scratchpad/ramen-master）、とんかつ/天ぷら/焼肉の本場複数化、米・貝甲殻・調味料の Wikipedia 出典の一次資料差し替え、寿司ジャンルの系統（郷土/型/現代/ネタ）付与、鶏の部位図の線画改善。

## 意図的な制約（勝手に外さない）

いずれも理由と解禁条件を `.doc` に記録済み。**必要になったら解禁する**もので、忘れられているわけではない。

| 封印 | 解禁条件 | 記録先 |
| :--- | :--- | :--- |
| **PostGIS** | 半径検索・距離ソートが要件化したとき | `.doc/10_system/01_architecture.md` §2.1 |
| **ORM（Prisma/Drizzle）** | 採用しない。`supabase gen types` + zod + View/RPC で構成する | 同 §2.3 |
| **認証（Supabase Auth）** | 制覇マップを実装するとき（フェーズ3） | 同 §3 |
| **写真・イラスト** | フェーズ2。スタイル確定プロセスを経てから | `.doc/40_operation/01_strategy.md` §2 |
| **コメント欄** | 設けない。交流はSNS側で起こす | 同 §4.1 |

座標は `lat`/`lng` の**素の数値列**で持つ（PostGIS解禁コストを低く保つため。`geography` 型で保存しない）。

## デザイン（2026-08 確定。実装時に必ず守る）

正典はデザインキャンバス（Artifact「Itadaki Atlas デザイン方向」の「画面」「ブランド」ページ）。要点:

| トークン | 値 |
| :--- | :--- |
| ブランド橙 | `#ff8f00`（図形アクセント・ピン●・主ボタン塗り） |
| 淡 / 濃 | `#ffc985` / `#e56000`（濃=テキストアクセント・リンク・章番号） |
| 文字 | 基本 `#5b4a37`・サブ `#7a6a58`（**真っ黒禁止**） |
| 紙 | `#fffdf7`（本文地）・罫 `#e3d9c8`〜`#6e5c47` |
| 地図 | 海 `#efe8da`・陸 `#fffdf7`・海岸線 `#c3b8a6`・実データ地形。**経緯線グリッド禁止**（座標表記1行は可） |
| 系統色（ラーメン内部・単一ジャンル時のみ） | 醤油 `#b06a1f`・味噌 `#e08a2e`・塩 `#7fa8c9`・豚骨 `#e8dcc8` |

- **色の規則**: 橙系に黒を混ぜない（濃くする時は色相を橙〜赤へ）。緑・紫・青系のアクセント禁止。ポップは色と記号で作り、見出しの明朝と紙の本文は維持
- **画面の層**: 紙の共通ヘッダー（SP 48px / PC 62px、橙ワードマーク）→ 橙のヒーロー（白文字）→ 明るい地図 → 紙の本文
- **記号**: ●料理（生まれた）/ ■食材（育てる）/ ◆仕込み（仕込む）/ ○本場（ここのは特別。中抜き=ここ生まれではない。塗り紙・リング橙・外周輪郭 `#5b4a37`）。ピンは橙3トーン+輪郭 `#5b4a37`。識別は形が主・色は従
- **ボタン**: 主=橙塗り+白字 / 副=紙+細罫（色付き淡塗りのボタン禁止）
- **禁止の継続**: ランキング・煽り・偽和風（筆文字/和柄/鳥居の朱）・行き止まり（全詳細に名前つきの次）
- **マーク（ロゴ）は未決**。ヘッダーは暫定で紋マーク（●■◆の三つ紋）を使用

## 情報構造（2026-08 確定）

詳細と編集規律は `ia-atlas-content` Skill が正典。骨子: **タグ一元+排他棚1本**（棚28: 料理10・食材9・仕込み9）／ジャンルは昇格制（同型20件以上+実在語。未満は棚内その他）／関係4語彙（源流/派生・兄弟・対比・使う⇄使われる）で行き止まり禁止／詳細ページ=カバー・目次・章Markdown・つながり（出典はUI非表示・内部検証データ）。

## 体験原則（2026-09 本番レビューから固定。UI変更の指示書に必ず転記する）

指摘の多くは「ユーザーの目で通しで触れば分かること」だった。以下は事例ではなく原則として書く。部隊への指示書は事例だけでなくこの原則を転記し、「この原則に照らして他に該当箇所は無いか」を実装者に問わせる。

1. **地図は主役だが、見ていない間は退く。** 絞り込み中・シートを開いて読んでいる間・県へ潜っている間は上部の帯（SP 38vh / PC 45vh）に縮め、戻したらフルサイズに復帰する
2. **選択の直後に必ず文脈を出す。** ジャンル・タグ・県を選んだ画面には、件数の前に「これは何か」（ジャンル総論・県の一行）を出す。一覧だけを見せない。そのため総論は全ジャンル必須（`content:lint` E7 で機械検査）
3. **ラベルは分類名ではなく土地との関係で言う。** 「ご当地」「Regional」は使わない。「生まれた土地」「いまの広がり（全国のチェーン／地域／その土地だけ）」「本場」で言う
4. **情報は3層で扱う。** 料理（土地から生まれた型）／食材（銘柄・品種・魚介＝土地に結びつく）／部位・ネタ（一頭のどこか・仕事＝土地に依らない図鑑）。同じ画面で層を混ぜて並べない。この3層は `genres.type`（`dish` / `ingredient` / `cut`）に対応し、地図ピンの記号 ●■◆（`food_items.type` と棚）とは別の軸。部位・ネタのアイテムはピン上は ■ のまま
5. **全国区の料理には受け皿と複数の本場を持たせる。** ご当地アイテムだけでなく料理そのもののアイテムを持ち、本場は2箇所以上を理由文つきで。「辿る」装置は2箇所以上のものだけを出す
6. **行き止まりを作らない。** 全ての詳細に名前つきの次があり、全ての一覧の末尾に次の入口がある
7. **文だけで伝わらないものは図にする。** 部位・系統・構造は SVG 線画（ブランド色・黒禁止）で補う

検品は「差分」ではなく「導線」で行う。UI を変えたら `ia-atlas-ux-reviewer` エージェント（`.claude/agents/`）に SP/PC の主要導線を実操作させ、固定の質問に答えさせてから報告する。

## 固有の実装方針

- **地図**: MapLibre GL JS + Protomaps（PMTiles）を R2 から配信。ベースマップとピン（自社データ）は別レイヤーで、アイテム追加でタイル再生成は不要
- **i18n**: `next-intl` / サブパス方式（`/ja/` `/en/`）。**自動リダイレクトはしない**（Platform `../.doc/10_system/10_growth_infra.md` §3）
- **Supabase の全件取得は必ずページングする。** PostgREST は既定で1,000行までしか返さない（2026-09-10、収録が1,867件になった時点でトップの索引が欠けた。「牛肉の部位」が30件のはずが19件など）。全件を返す想定のクエリは `src/lib/supabase/fetchAll.ts` の `fetchAllRows`（1,000件ずつ `.range()` して結合する共通ヘルパー）を通す。1ジャンル・1県のように現状は1,000件未満が確実なものは無理に通さなくてよいが、将来1,000件を超えうるもの（ジャンル・タグ等）は迷ったら通す
- **名称表記**: 全アイテムで「**日本語名 — ローマ字 — 英訳（説明訳）**」の三点セット。装飾ではなく機能要件
- **リンクをボタン風にする場合**は `Button` コンポーネントではなく `buttonVariants()` をクラスとして当てる。`Button` + `nativeButton={false}` は `<a>` に `role="button"` を付け、スクリーンリーダーがリンクをボタンとして読み上げるため（WCAG 2.2 AA を要件に掲げている）
